# ═══════════════════════════════════════════════════════════════════
# ChatGLM3-6B 模型覆盖配置
# 仅包含与 default.yaml 不同的参数
# ChatGLM3 即为默认基线，此文件基本为空
# ═══════════════════════════════════════════════════════════════════

# ChatGLM3 的参数已经是 default.yaml 的基线值，无需额外覆盖
# 如需调整，在此处添加对应的键即可，例如:
#
# common:
#   pretrain_LM: /your/custom/path/
#
# classification:
#   meld:
#     batch_size: 32
#     learning_rate: 1.0e-4
