# ═══════════════════════════════════════════════════════════════════
# Qwen3.5-2B 模型覆盖配置
# 仅包含与 default.yaml 不同的参数，其余继承默认值
# 初始模板，请根据实验结果调整
# ═══════════════════════════════════════════════════════════════════

common:
  pretrain_LM: /root/autodl-tmp/models/Qwen/Qwen-3.5-25B/
  lora_target_modules: q_proj,v_proj

# 目前使用 default.yaml 的默认训练参数
# 根据实验结果，在此处添加需要覆盖的参数，例如:
# classification:
#   meld:
#     batch_size: 16
#     learning_rate: 5.0e-4
